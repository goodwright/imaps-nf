{
    "title": "Demultiplex and Analyse",
    "description": "Demultiplexes a reads file into individual reads files, and analyses each one in parallel.",
    "type": "object",
    "definitions": {
        "demux_options": {
            "title": "Demultiplex options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Files needed for the initial demultiplexing.",
            "properties": {
                "csv": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "pattern": "csv|xlsx",
                    "imapsType": "annotation",
                    "required": true,
                    "description": "A CSV file describing the contents of the multiplexed reads file."
                },
                "multiplexed_fastq": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/plain",
                    "pattern": "fq\\.gz|fastq\\.gz",
                    "imapsType": "multiplexed",
                    "required": true,
                    "description": "A multiplexed reads file containing reads from multiple experiments, distinguishable by barcode sequence."
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/definitions/file_options"
        }
    ]
}