{
    "file_options": {
        "name": "File options",
        "description": "Files needed to run the module.",
        "properties": {
            "bam": {
                "type": "file",
                "pattern": "bam",
                "required": true,
                "description": "A BAM file with duplicated UMIs."
            },
            "bai": {
                "type": "file",
                "pattern": "bai",
                "required": true,
                "description": "A BAM index for the BAM file."
            }
        }
    },
    "optional_options": {
        "name": "Optional parameters",
        "description": "Parameters for customising the behaviour of UMICollapse.",
        "properties": {
            "umi_separator": {
                "type": "string",
                "required": false,
                "description": "The string used to denote the UMI in the read headers (if not 'rbc:')."
            }
        }
    }
}
